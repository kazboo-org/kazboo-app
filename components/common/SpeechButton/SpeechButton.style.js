import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    height: 40,
    paddingVertical: 5,
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
  },
});

export default styles;
